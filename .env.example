# Copy this file to .env and adjust only if needed.
TZ=Asia/Shanghai

# Used by the macOS port-mapping overlay. The Debian host-network overlay
# keeps Home Assistant's native container port (8123).
HA_HTTP_PORT=8123
